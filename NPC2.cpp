#include <iostream>

void person21(int& qwest21, int& killCount, int& secretIngredient, int& keys, int& killCountQwest)
{
    if (qwest21 == '0')
    {
        std::cout << "Ты кто? Выживший? Хм... Вижу, ты умеешь обращаться с оружием. Слушай, я создатель этой сыворотки. Да, я тот самый сумасшедший учёный.\n"
            "У меня остался последний образец Астралиума - ключевой катализатор превращения. В чистом виде он может дать временный иммунитет.\n"
            "Но тебе он ничего не даст - нужна точная дозировка и активация ультразвуком, этому не научишься за пять минут. Доверю его только тому, кто докажет свою эффективность как истребитель.\n"
            "Принеси мне пятнадцать пар ушей этих тварей. Тогда поговорим.\n";
        qwest21 = '1';
        killCountQwest = killCount;
    }
    else
    {
        killCountQwest += killCount - killCountQwest;
        if (qwest21 == '1' && killCountQwest < 15)
        {
            std::cout << "Где уши? Я вижу только " << killCountQwest << ". Мы договаривались на пятнадцать. Не пытайся меня торопить или уговаривать.\n"
                "Пятнадцать - или никакого Астралиума. Мне нужен результат, не твои оправдания.\n";
        }
        else if (qwest21 == '1' && killCountQwest >= 15)
        {
            std::cout << "Вот он. Астралиум. Не вздумай вскрывать ампулу без оборудования - это смертельно. И не пытайся использовать его как оружие - без активации он просто фиолетовая жидкость.\n"
                "Может, встретишь других учёных из моего отдела - им покажи. Они поймут. А теперь оставь меня. Ну и так уж и быть дам тебе ещё кое-что: два ключа, авось помогут. Я всё сказал.\n";
            secretIngredient = 1;
            keys += 2;
            qwest21 = '2';
        }
        else if (qwest21 == '2')
        {
            std::cout << "Ты что, не понял с первого раза? Я дал тебе всё, что мог. Не приставай ко мне со своими вопросами.\n"
                "Используй Астралиум с умом. Если найдёшь кого-то, кто разбирается в биохимии... тогда может быть смысл. А так - просто носи с собой как талисман.\n";
        }
    }
}

void person22(int& qwest22, int& hasIngredient, int& bow)
{
    if (qwest22 == '0')
    {
        // Первая реплика - первая встреча
        std::cout << "Кто ты? Ну впрочем, у меня есть дело к тебе, ты наверника уже видел этих зомби, так вот, мы с моим другом сделали... кое-что. И мне это нужно чтобы убивать зомби. \n" << std::endl;
        qwest22 = '1';
    }
    else if (qwest22 == '1' && hasIngredient == 0)
    {
        // Вторая реплика - игрок пришёл без ингредиента
        std::cout << "[Вторая реплика - спрашивает про ингредиент, которого нет]" << std::endl;
    }
    else if (qwest22 == '1' && hasIngredient == 1 && bow == 0)
    {
        // Третья реплика - игрок пришёл с ингредиентом, получает предмет
        std::cout << "[Третья реплика - забирает ингредиент, даёт награду]" << std::endl;
        bow = 1;
        qwest22 = '2';
    }
    else if (qwest22 == '2')
    {
        // Четвёртая реплика - после получения предмета
        std::cout << "[Четвёртая реплика - прощальные слова]" << std::endl;
    }
}